## Quick demo payload you can paste into the frontend

Language: javascript

Code:
```javascript
function add(a, b) {
  return a + b
}
```

Expected: JSON response with summary, possible issues (none for this trivial example), suggestions, and optional patch/tests.
